machine:
  java:
    version: oraclejdk8

compile:
  override:
    - ./package.sh

test:
  override:
    - echo "test"

deployment:
  production:
    branch: master
    commands:
      - aws s3 sync target/ s3://toast.sh/ --quiet --acl public-read
