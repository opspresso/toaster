machine:
  java:
    version: oraclejdk8

dependencies:
  post:
    - ~/toaster/toast.sh config circle local nalbam $TOAST_TOKEN
    - ~/toaster/toast.sh init master

compile:
  override:
    - ./package.sh

test:
  override:
    - echo "test"

deployment:
  production:
    branch: master
    commands:
      - aws s3 sync target/ s3://toast.sh/ --quiet --acl public-read
