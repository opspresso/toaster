machine:
  timezone: Asia/Seoul

compile:
  override:
    - ./package.sh

test:
  override:
    - ls -al target

deployment:
  production:
    branch: master
    commands:
      - ./publish.sh toast.sh
      - ./publish.sh repo.toast.sh

general:
  artifacts:
    - target
