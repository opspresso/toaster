machine:
  java:
    version: oraclejdk8

dependencies:
  post:
    - ~/toaster/toast.sh config circle local nalbam $TOAST_TOKEN
    - ~/toaster/toast.sh init slave

compile:
  override:
    - ./package.sh

test:
  override:
    - ls -al target

deployment:
  production:
    branch: master
    commands:
      - ./publish.sh
