machine:
  java:
    version: oraclejdk8

compile:
  override:
    - ./package.sh

test:
  override:
    - echo "test"

deployment:
  production:
    branch: master
    commands:
      - ./publish.sh
