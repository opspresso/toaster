#
# CircleCI 2.0
#
version: 2
jobs:
  package:
    docker:
      - image: circleci/python
    working_directory: /tmp/works
    steps:
      - checkout
      - run:
          name: Package
          command: |
            chmod 755 *.sh
            ./build.sh package
            ./build-package.sh
      - persist_to_workspace:
          root: /tmp/works
          paths:
            - target/*
            - build.sh
  publish:
    docker:
      - image: opspresso/awscli
    steps:
      - attach_workspace:
          at: works
      - run:
          name: Publish
          command: |
            chmod 755 ./works/build.sh
            ./works/build.sh publish ./works
  release:
    docker:
      - image: circleci/golang
    steps:
      - attach_workspace:
          at: works
      - run:
          name: Release
          command: |
            chmod 755 ./works/build.sh
            ./works/build.sh release ./works
  slack:
    docker:
      - image: circleci/python
    steps:
      - attach_workspace:
          at: works
      - run:
          name: Slack
          command: |
            chmod 755 ./works/build.sh
            ./works/build.sh slack ./works

workflows:
  version: 2
  apply:
    jobs:
      - package
      - publish:
          requires:
            - package
      - release:
          requires:
            - package
      - slack:
          requires:
            - release
