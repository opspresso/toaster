name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
      with:
        fetch-depth: 1
    - name: Build
      run: |
        pwd
        mkdir target
        curl -sLo target/builder.sh toast.sh/builder
        ls -al
        ls -al target
